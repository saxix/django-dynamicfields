============
Installation
============

At the command line::

    $ easy_install drf-dynamicfields

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv drf-dynamicfields
    $ pip install drf-dynamicfields
